import React from "react"
import Button from "../Button/Button";

const ButtonGroup = (props) => {
  return (
    <div>
      <Button buttonType='save'>d</Button>
      <Button buttonType='save'>p</Button>
      <Button buttonType='save'>c</Button>
      <Button buttonType='save'>s</Button>
    </div>
  )
};

export default ButtonGroup;
